language: python
python:
  - "3.5"
  - "3.6"
  - "3.7-dev"

before_install:
  - sudo apt-get update
  - sudo apt-get install -y xorg libcairo2-dev python3-gi gir1.2-gtk-3.0 gir1.2-glib-2.0

install:
  - pip3 install ".[test]"

script:
  - xvfb-run pytest
